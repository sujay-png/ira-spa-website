<section class="about-section">
	<div class="about-image">
	<div class="container">
		<img
			src="https://iraspa.in/wp-content/uploads/2026/03/massage-in-mangalore.webp"
			alt="Ira Spa"
		/>
		<div class="badge">
			<span class="badge-number">24</span>
			<span class="badge-caption">Years of Bliss</span>
		</div>
	</div>
</div>

	<div class="about-content">
		<h3>Get to know us</h3>
		<h1>About Ira Spa</h1>
		<p>
			Welcome to IRA Spa, your destination for massage in Mangalore. Get
			deep relief and relaxation from pain or stress in your shoulder,
			neck, upper and lower back, legs, feet, and overall body with
			effective massage techniques that calm your nervous system,
			release stress hormones, soothe muscle aches, ease anxiety and
			depression, and keep your body, mind, and spirit rejuvenated.
		</p>
		<p>
			The spa's interior setting has been designed to create a peaceful
			atmosphere and sense of calmness from the moment you enter. It's
			time to take a break from your daily routine and invest in your
			mental and physical well-being. Whether you're looking for
			massage therapy or beauty spa sessions, you'll experience
			personalized service from our expert therapists at IRA Spa.
		</p>
		<div class="form-group button-group">
			<button type="submit" class="discover-btn">Discover Now</button>
		</div>
	</div>
</section>

<style>
	/*
	  The badge sits on top of the photo, not the page background, so it
	  keeps its own fixed gold/dark palette in both themes on purpose —
	  don't swap these for --bg-color/--text-color or it'll blend into
	  the image or disappear depending on theme.
	*/
	.badge {
		position: absolute;
		bottom: -20px;
		right: -20px;
		background-color: #f0c96b;
		padding: 24px 32px;
		border-radius: 4px;
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		line-height: 1;
		box-shadow: 0 8px 24px rgba(0, 0, 0, 0.25);
	}

	.badge-number {
		font-family: "Georgia", "Times New Roman", serif;
		font-size: 3.2em;
		font-weight: 700;
		color: #1a1a1a;
	}

	.badge-caption {
		font-family: "Poppins", sans-serif;
		font-size: 0.8em;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 1.5px;
		color: #1a1a1a;
		margin-top: 4px;
	}

	.about-image {
		position: relative;
		overflow: hidden;
		border-radius: 13px;
	}

	.container {
		max-width: none;
		padding: 40px 40px 0 40px;
		border-radius: 4px;
		position: relative;
	}

	.container::before {
		content: "";
		position: absolute;
		top: -12px;
		left: -12px;
		width: 40px;
		height: 40px;
		border-radius: 4px;
	}

	.about-section {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 48px;
		align-items: center;
		max-width: 1200px;
		margin: 0 auto;
		padding: 60px 20px;
		background-color: var(--bg-color, transparent);
	}

	.about-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 4px;
		display: block;
		border: 3px solid gold;
		border-radius: 13px;
		padding: 14px;
		object-fit: cover;
		transition: transform 0.8s ease-in-out, border-color 0.8s ease-in-out;
	}

	.about-image:hover img {
		transform: scale(1.15);
		border-color: transparent;
	}

	.about-content h3 {
		font-family: "Poppins", sans-serif;
		font-size: 0.95em;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 1px;
		color: var(--accent-color, #cfa23c);
		margin: 0 0 8px 0;
	}

	.about-content h1 {
		font-family: "Poppins", sans-serif;
		font-size: 2.2em;
		font-weight: 700;
		color: var(--text-color, #1a1a1a);
		margin: 0 0 20px 0;
	}

	.about-content p {
		font-family: "Poppins", sans-serif;
		font-size: 1em;
		line-height: 1.7;
		color: var(--text-muted, #555);
		margin: 0 0 20px 0;
	}

	.form-group {
		display: flex;
		flex-direction: column;
	}

	.form-group label {
		font-family: "Poppins", sans-serif;
		font-size: 0.85em;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.5px;
		color: var(--text-muted, #444);
		margin-bottom: 8px;
	}

	.form-group input,
	.form-group select {
		font-family: "Poppins", sans-serif;
		font-size: 0.95em;
		color: var(--text-color, #333);
		background-color: var(--surface-color, #faf6f2);
		border: 1.5px solid var(--border-color, #eee0d3);
		border-radius: 6px;
		padding: 12px 14px;
		outline: none;
		transition: border-color 0.2s ease, box-shadow 0.2s ease;
	}

	.form-group input:focus,
	.form-group select:focus {
		border-color: var(--accent-color, #cfa23c);
		box-shadow: 0 0 0 3px rgba(207, 162, 60, 0.15);
	}

	.form-group input::placeholder {
		color: var(--text-muted, #a8a29a);
	}

	.button-group {
		align-items: flex-start;
	}

	.discover-btn {
		background-color: var(--accent-color, #cfa23c);
		color: white;
		font-family: "Poppins", sans-serif;
		font-size: 0.95em;
		font-weight: 700;
		letter-spacing: 1px;
		text-transform: uppercase;
		border: none;
		border-radius: 6px;
		padding: 14px 32px;
		cursor: pointer;
		transition: background-color 0.2s ease, transform 0.15s ease;
	}

	.discover-btn:hover {
		background-color: #b88f32;
		transform: translateY(-1px);
	}

	@media (max-width: 800px) {
		.about-section {
			grid-template-columns: 1fr;
		}
	}
</style>