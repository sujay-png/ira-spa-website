---
// Deprecated: dynamic routes must live under src/pages to be routable by Astro,
// so this file was never actually reachable. The real route now lives at
// src/pages/services/[slug].astro. Kept as an empty stub because this
// environment's mounted folder does not allow file deletion.
---
